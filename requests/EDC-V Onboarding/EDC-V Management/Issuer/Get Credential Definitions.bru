meta {
  name: Get Credential Definitions
  type: http
  seq: 3
}

post {
  url: {{issuerUrl}}/admin/participants/issuer/credentialdefinitions/query
  body: json
  auth: inherit
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
