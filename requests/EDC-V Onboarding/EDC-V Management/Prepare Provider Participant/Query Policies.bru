meta {
  name: Query Policies
  type: http
  seq: 8
}

post {
  url: {{cpBaseUrl}}/v5beta/participants/{{provider_id}}/policydefinitions/request
  body: json
  auth: inherit
}

body:json {
  {
    "@context": [
      "https://w3id.org/edc/connector/management/v2"
    ],
    "@type": "QuerySpec"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
