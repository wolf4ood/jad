meta {
  name: Get Attestation Definitions
  type: http
  seq: 5
}

post {
  url: {{issuerUrl}}/admin/participants/issuer/attestations/query
  body: json
  auth: inherit
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
