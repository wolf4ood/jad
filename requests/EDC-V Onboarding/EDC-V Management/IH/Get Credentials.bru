meta {
  name: Get Credentials
  type: http
  seq: 2
}

get {
  url: {{identityHubUrl}}/participants/{{tx_provider_context_id}}/credentials
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
