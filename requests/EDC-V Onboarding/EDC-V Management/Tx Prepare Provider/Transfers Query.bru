meta {
  name: Transfers Query
  type: http
  seq: 13
}

post {
  url: {{txBaseUrl}}/v4beta/transferprocesses/request
  body: json
  auth: inherit
}

body:json {
  {
    "@context": [
      "https://w3id.org/edc/connector/management/v2"
    ],
    "@type": "QuerySpec"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
