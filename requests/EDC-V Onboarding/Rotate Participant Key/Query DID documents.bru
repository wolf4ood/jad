meta {
  name: Query DID documents
  type: http
  seq: 2
}

post {
  url: {{identityHubUrl}}/participants/{{tx_provider_context_id}}/dids/query
  body: json
  auth: inherit
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
