meta {
  name: Get Cert Asset
  type: http
  seq: 3
}

get {
  url: {{cpBaseUrl}}/api/mgmt/v4alpha/participants/{{provider_id}}/assets/{{ASSET_CERT_ID}}
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
