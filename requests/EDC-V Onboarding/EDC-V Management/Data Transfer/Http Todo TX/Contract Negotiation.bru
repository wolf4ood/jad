meta {
  name: Contract Negotiation
  type: http
  seq: 1
}

post {
  url: {{cpBaseUrl}}/api/mgmt/v1alpha/participants/{{consumer_id}}/data
  body: json
  auth: inherit
}

body:json {
  {
      "providerId":"did:web:identityhub.edc-v.svc.cluster.local%3A7083:provider",
      "policyId": "{{POLICY_ID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
