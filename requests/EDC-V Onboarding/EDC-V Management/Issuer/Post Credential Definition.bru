meta {
  name: Post Credential Definition
  type: http
  seq: 4
}

post {
  url: {{issuerUrl}}/admin/participants/issuer/credentialdefinitions
  body: json
  auth: inherit
}

body:json {
  {
    "id": "gov-credential-def",
    "attestations": [
      "holder-attestation-def"
    ],
    "rules": [],
    "mappings": [
      {
        "input": "contractVersion",
        "output": "credentialSubject.contractVersion",
        "required": true
      }
    ],
    "format": "VC1_0_JWT",
    "credentialType": "DataExchangeGovernanceCredential",
    "jsonSchema": "{}",
    "jsonSchemaUrl": "https://example.com/schema/membership-credential.json",
    "validity": 604800
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
